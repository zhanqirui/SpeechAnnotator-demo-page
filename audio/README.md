This folder contains the long audio recordings and their JSON annotations.

The static demo page uses `audio-demo.js` to display one continuous 60-second
excerpt from each audio file with synchronized subtitles and metadata.
